#include <stdio.h>

// Punto 1//

int main(void)
{

    int contador = 1;
    int a = 20;
    while (contador <= a)
    {
        printf("\n %d", contador);
        contador = contador + 1;
    }
    system("pause");
    return 0;
}